# 数据库设计文档

## 1. 数据库选型与连接配置

本项目采用 **SQLAlchemy 2.x** 作为 ORM 框架，数据库层支持 **PostgreSQL** 与 **SQLite** 两种存储引擎，根据环境变量与可用依赖自动降级。连接配置集中在 `backend/database.py` 中实现。

### 1.1 URL 解析与降级策略

`database.py` 通过 `_resolve_database_url()` 函数按以下优先级解析数据库连接：

1. 若未配置 `DATABASE_URL`，默认使用本地 SQLite：`sqlite:///./ecommerce_ai.db`。
2. 若 `DATABASE_URL` 以 `sqlite` 开头，直接使用该配置。
3. 若 `DATABASE_URL` 以 `postgresql` 开头，则检测 `psycopg2` 是否已安装，并尝试执行 `SELECT 1` 验证连通性；任意环节失败均降级到 SQLite。
4. 其他协议字符串则原样返回。

```python
# backend/database.py

def _resolve_database_url() -> str:
    """解析数据库 URL，PostgreSQL 不可用时降级到 SQLite"""
    configured_url = os.getenv("DATABASE_URL", "")

    # 如果没有配置，直接用 SQLite
    if not configured_url:
        logger.info("未配置 DATABASE_URL，使用 SQLite: ./ecommerce_ai.db")
        return "sqlite:///./ecommerce_ai.db"

    # 如果配置的是 SQLite，直接用
    if configured_url.startswith("sqlite"):
        return configured_url

    # 如果配置的是 PostgreSQL，检查 psycopg2 是否可用
    if configured_url.startswith("postgresql"):
        try:
            import psycopg2  # noqa: F401
            # 尝试连接
            engine = create_engine(configured_url, pool_pre_ping=True)
            with engine.connect() as conn:
                conn.execute(__import__("sqlalchemy").text("SELECT 1"))
            logger.info("使用 PostgreSQL 数据库")
            return configured_url
        except ImportError:
            logger.warning("psycopg2 未安装，降级使用 SQLite: ./ecommerce_ai.db")
            return "sqlite:///./ecommerce_ai.db"
        except Exception as e:
            logger.warning(f"PostgreSQL 连接失败 ({e})，降级使用 SQLite: ./ecommerce_ai.db")
            return "sqlite:///./ecommerce_ai.db"

    # 其他情况使用配置的 URL
    return configured_url
```

### 1.2 引擎创建与连接参数

- **SQLite**：通过 `connect_args={"check_same_thread": False}` 允许跨线程使用同一连接；并在 `connect` 事件监听中执行 `PRAGMA foreign_keys=ON`，显式启用外键约束。
- **PostgreSQL**：启用连接池，配置 `pool_size=10`、`max_overflow=20`、`pool_pre_ping=True`，开发环境开启 SQL 回显。

```python
# backend/database.py

if DATABASE_URL.startswith("sqlite"):
    engine = create_engine(
        DATABASE_URL,
        echo=False,
        connect_args={"check_same_thread": False},
    )

    # SQLite 启用外键约束
    @event.listens_for(engine, "connect")
    def set_sqlite_pragma(dbapi_connection, connection_record):
        cursor = dbapi_connection.cursor()
        cursor.execute("PRAGMA foreign_keys=ON")
        cursor.close()
else:
    engine = create_engine(
        DATABASE_URL,
        pool_size=10,
        max_overflow=20,
        pool_pre_ping=True,
        echo=os.getenv("APP_ENV", "development") == "development",
    )
```

### 1.3 会话管理

项目提供两种会话获取方式：

- `get_db()`：生成器形式，适用于 FastAPI 等依赖注入场景，使用方负责提交。
- `get_db_context()`：上下文管理器形式，退出时自动提交，异常时自动回滚。

所有模型继承自 `Base = DeclarativeBase`，统一由 `Base.metadata` 管理。

```python
# backend/database.py

class Base(DeclarativeBase):
    pass


def get_db() -> Session:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


@contextmanager
def get_db_context() -> Session:
    db = SessionLocal()
    try:
        yield db
        db.commit()
    except Exception:
        db.rollback()
        raise
    finally:
        db.close()
```

---

## 2. 数据表清单与关系总览

### 2.1 表清单

| 序号 | 表名（物理） | 模型类 | 业务含义 |
|------|-------------|--------|---------|
| 1 | `users` | `User` | 用户与商家管理员统一账户 |
| 2 | `user_addresses` | `UserAddress` | 用户收货地址 |
| 3 | `user_favorites` | `UserFavorite` | 用户收藏商品 |
| 4 | `browse_histories` | `BrowseHistory` | 用户浏览记录 |
| 5 | `products` | `Product` | 商品主信息 |
| 6 | `reviews` | `Review` | 商品评论 |
| 7 | `cart_items` | `CartItem` | 购物车条目 |
| 8 | `orders` | `Order` | 订单主信息 |
| 9 | `order_items` | `OrderItem` | 订单中的商品明细 |
| 10 | `customer_service_messages` | `CustomerServiceMessage` | 客服消息记录 |
| 11 | `knowledge_entries` | `KnowledgeEntry` | RAG 知识库条目 |
| 12 | `qa_records` | `QARecord` | 用户问答记录 |

### 2.2 ER 图文字描述

```text
users ||--o{ user_addresses         : 一个用户拥有多个收货地址
users ||--o{ user_favorites         : 一个用户可收藏多个商品
users ||--o{ browse_histories       : 一个用户产生多条浏览记录
users ||--o{ orders                 : 一个用户拥有多个订单
users ||--o{ customer_service_messages : 一个用户发起多条客服消息（user_id）
users ||--o{ customer_service_messages : 一个用户可作为多条消息的发送者（sender_id）
users ||--o{ qa_records             : 一个用户可有多条问答记录

products ||--o{ reviews             : 一个商品有多条评论（级联删除）
products ||--o{ cart_items          : 一个商品可被多个用户加入购物车
products ||--o{ customer_service_messages : 一个商品对应多条客服咨询
products ||--o{ knowledge_entries   : 一个商品可有多条知识库条目
products ||--o{ qa_records          : 一个商品可产生多条问答记录

orders ||--|{ order_items          : 一个订单包含多个商品明细（级联删除）
```

- 主键：所有表均采用自增整数主键 `id`。
- 逻辑删除：未设计 `deleted_at` 字段，删除行为通过 SQLAlchemy 级联 `cascade="all, delete-orphan"` 实现。
- 数据快照：订单中的地址与商品信息使用 JSON 文本快照保存，避免商品/地址后续变更影响历史订单。

---

## 3. 每张表的结构

### 3.1 users（用户表）

统一存储普通用户与商家管理员，通过 `role` 字段区分身份。

```python
# backend/models/user.py

class User(Base):
    """用户表 - 统一存储普通用户和商家管理员"""
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    username: Mapped[str] = mapped_column(String(100), unique=True, comment="登录账号")
    password_hash: Mapped[str] = mapped_column(String(256), comment="密码哈希")
    nickname: Mapped[str] = mapped_column(String(100), nullable=True, comment="昵称")
    role: Mapped[str] = mapped_column(String(20), default="user", comment="角色: user/merchant")
    avatar: Mapped[str] = mapped_column(String(500), nullable=True, comment="头像URL")
    phone: Mapped[str] = mapped_column(String(20), nullable=True, comment="手机号")
    email: Mapped[str] = mapped_column(String(200), nullable=True, comment="邮箱")
    is_active: Mapped[bool] = mapped_column(Boolean, default=True, comment="是否启用")
    display_id: Mapped[str] = mapped_column(String(64), nullable=True, unique=True, comment="展示ID")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now, onupdate=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | `PRIMARY KEY AUTOINCREMENT` | 自增主键 |
| `username` | `String(100)` | `UNIQUE` | 登录账号 |
| `password_hash` | `String(256)` | `NOT NULL` | 密码哈希 |
| `nickname` | `String(100)` | 可空 | 昵称 |
| `role` | `String(20)` | 默认 `user` | 角色：`user` / `merchant` |
| `avatar` | `String(500)` | 可空 | 头像 URL |
| `phone` | `String(20)` | 可空 | 手机号 |
| `email` | `String(200)` | 可空 | 邮箱 |
| `is_active` | `Boolean` | 默认 `True` | 是否启用 |
| `display_id` | `String(64)` | `UNIQUE`、可空 | 对外展示 ID |
| `created_at` | `DateTime` | 默认 `datetime.now` | 创建时间 |
| `updated_at` | `DateTime` | 默认并更新时刷新 | 更新时间 |

### 3.2 user_addresses（用户收货地址）

```python
# backend/models/user.py

class UserAddress(Base):
    """用户收货地址"""
    __tablename__ = "user_addresses"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, comment="用户ID")
    name: Mapped[str] = mapped_column(String(100), comment="收货人姓名")
    phone: Mapped[str] = mapped_column(String(20), comment="联系电话")
    province: Mapped[str] = mapped_column(String(50), comment="省")
    city: Mapped[str] = mapped_column(String(50), comment="市")
    district: Mapped[str] = mapped_column(String(50), comment="区")
    detail: Mapped[str] = mapped_column(String(500), comment="详细地址")
    is_default: Mapped[bool] = mapped_column(Boolean, default=False, comment="是否默认地址")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `user_id` | `Integer` | `NOT NULL` | 所属用户 |
| `name` | `String(100)` | `NOT NULL` | 收货人姓名 |
| `phone` | `String(20)` | `NOT NULL` | 联系电话 |
| `province` | `String(50)` | `NOT NULL` | 省 |
| `city` | `String(50)` | `NOT NULL` | 市 |
| `district` | `String(50)` | `NOT NULL` | 区 |
| `detail` | `String(500)` | `NOT NULL` | 详细地址 |
| `is_default` | `Boolean` | 默认 `False` | 是否默认地址 |
| `created_at` | `DateTime` | 默认当前时间 | 创建时间 |

### 3.3 user_favorites（用户收藏）

```python
# backend/models/user.py

class UserFavorite(Base):
    """用户收藏"""
    __tablename__ = "user_favorites"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, comment="用户ID")
    product_id: Mapped[int] = mapped_column(Integer, comment="商品ID")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `user_id` | `Integer` | `NOT NULL` | 用户 ID |
| `product_id` | `Integer` | `NOT NULL` | 商品 ID |
| `created_at` | `DateTime` | 默认当前时间 | 收藏时间 |

### 3.4 browse_histories（浏览记录）

```python
# backend/models/user.py

class BrowseHistory(Base):
    """浏览记录"""
    __tablename__ = "browse_histories"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, comment="用户ID")
    product_id: Mapped[int] = mapped_column(Integer, comment="商品ID")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `user_id` | `Integer` | `NOT NULL` | 用户 ID |
| `product_id` | `Integer` | `NOT NULL` | 商品 ID |
| `created_at` | `DateTime` | 默认当前时间 | 浏览时间 |

### 3.5 products（商品表）

```python
# backend/models/product.py

class Product(Base):
    """商品表"""

    __tablename__ = "products"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    platform: Mapped[str] = mapped_column(String(50), nullable=True, default="manual", comment="来源平台（jd/taobao/manual）")
    product_id: Mapped[str] = mapped_column(String(100), nullable=True, comment="平台商品ID（手动创建时为空）")
    name: Mapped[str] = mapped_column(String(500), comment="商品名称")
    category: Mapped[str] = mapped_column(String(200), nullable=True, default="", comment="商品类目")
    price: Mapped[float] = mapped_column(Float, nullable=True, default=0, comment="价格")
    original_price: Mapped[float] = mapped_column(Float, nullable=True, comment="原价")
    brand: Mapped[str] = mapped_column(String(200), nullable=True, default="", comment="品牌")
    selling_points: Mapped[str] = mapped_column(Text, nullable=True, default="", comment="卖点描述")
    image_url: Mapped[str] = mapped_column(String(500), nullable=True, default="", comment="主图URL")
    image_urls: Mapped[list[str] | None] = mapped_column(JSON, nullable=True, comment="多图URL列表")
    videos: Mapped[list[str] | None] = mapped_column(JSON, nullable=True, comment="视频URL列表")
    detail_url: Mapped[str] = mapped_column(String(500), nullable=True, default="", comment="详情URL")
    source_url: Mapped[str] = mapped_column(String(500), nullable=True, default="", comment="来源链接")
    specs: Mapped[str] = mapped_column(Text, nullable=True, default="", comment="规格参数")
    sales_count: Mapped[int] = mapped_column(Integer, nullable=True, default=0, comment="销量")
    rating: Mapped[float] = mapped_column(Float, nullable=True, default=5.0, comment="评分")
    review_count: Mapped[int] = mapped_column(Integer, default=0, comment="评论数")
    display_id: Mapped[str] = mapped_column(String(64), nullable=True, unique=True, comment="展示ID")
    is_published: Mapped[bool] = mapped_column(Boolean, default=True, comment="是否上架")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now, onupdate=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `platform` | `String(50)` | 默认 `manual` | 来源平台：`jd` / `taobao` / `manual` |
| `product_id` | `String(100)` | 可空 | 平台商品 ID |
| `name` | `String(500)` | `NOT NULL` | 商品名称 |
| `category` | `String(200)` | 可空，默认空 | 商品类目 |
| `price` | `Float` | 可空，默认 `0` | 价格 |
| `original_price` | `Float` | 可空 | 原价 |
| `brand` | `String(200)` | 可空，默认空 | 品牌 |
| `selling_points` | `Text` | 可空，默认空 | 卖点描述 |
| `image_url` | `String(500)` | 可空，默认空 | 主图 URL |
| `image_urls` | `JSON` | 可空 | 多图 URL 列表 |
| `videos` | `JSON` | 可空 | 视频 URL 列表 |
| `detail_url` | `String(500)` | 可空，默认空 | 详情 URL |
| `source_url` | `String(500)` | 可空，默认空 | 来源链接 |
| `specs` | `Text` | 可空，默认空 | 规格参数 |
| `sales_count` | `Integer` | 可空，默认 `0` | 销量 |
| `rating` | `Float` | 可空，默认 `5.0` | 评分 |
| `review_count` | `Integer` | 默认 `0` | 评论数 |
| `display_id` | `String(64)` | `UNIQUE`、可空 | 展示 ID |
| `is_published` | `Boolean` | 默认 `True` | 是否上架 |
| `created_at` | `DateTime` | 默认当前时间 | 创建时间 |
| `updated_at` | `DateTime` | 默认并更新时刷新 | 更新时间 |

### 3.6 reviews（评论表）

```python
# backend/models/review.py

class Review(Base):
    """评论表"""

    __tablename__ = "reviews"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    product_id: Mapped[int] = mapped_column(Integer, ForeignKey("products.id"), comment="关联商品ID")
    platform_review_id: Mapped[str] = mapped_column(String(100), nullable=True, default=lambda: uuid.uuid4().hex[:16], comment="平台评论ID")
    content: Mapped[str] = mapped_column(Text, comment="评论内容")
    rating: Mapped[int] = mapped_column(Integer, default=5, comment="评分（1-5）")
    user_name: Mapped[str] = mapped_column(String(100), nullable=True, default="匿名用户", comment="用户名")
    user_id: Mapped[int | None] = mapped_column(Integer, nullable=True, comment="关联用户ID")
    image_urls: Mapped[list[str] | None] = mapped_column(JSON, nullable=True, comment="评价图片URL列表")
    videos: Mapped[list[str] | None] = mapped_column(JSON, nullable=True, comment="评价视频URL列表")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `product_id` | `Integer` | `ForeignKey("products.id")` | 关联商品 ID |
| `platform_review_id` | `String(100)` | 可空，默认 UUID 前 16 位 | 平台评论 ID |
| `content` | `Text` | `NOT NULL` | 评论内容 |
| `rating` | `Integer` | 默认 `5` | 评分 1-5 |
| `user_name` | `String(100)` | 可空，默认匿名 | 用户名 |
| `user_id` | `Integer` | 可空 | 关联用户 ID |
| `image_urls` | `JSON` | 可空 | 评价图片 URL 列表 |
| `videos` | `JSON` | 可空 | 评价视频 URL 列表 |
| `created_at` | `DateTime` | 默认当前时间 | 评论时间 |

### 3.7 cart_items（购物车）

```python
# backend/models/shopping.py

class CartItem(Base):
    """购物车"""
    __tablename__ = "cart_items"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, comment="用户ID")
    product_id: Mapped[int] = mapped_column(Integer, ForeignKey("products.id"), comment="商品ID")
    quantity: Mapped[int] = mapped_column(Integer, default=1, comment="数量")
    selected: Mapped[bool] = mapped_column(Boolean, default=True, comment="是否选中")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `user_id` | `Integer` | `NOT NULL` | 用户 ID |
| `product_id` | `Integer` | `ForeignKey("products.id")` | 商品 ID |
| `quantity` | `Integer` | 默认 `1` | 数量 |
| `selected` | `Boolean` | 默认 `True` | 是否选中 |
| `created_at` | `DateTime` | 默认当前时间 | 加入购物车时间 |

### 3.8 orders（订单表）

```python
# backend/models/shopping.py

class Order(Base):
    """订单"""
    __tablename__ = "orders"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    order_no: Mapped[str] = mapped_column(String(64), unique=True, comment="订单号")
    user_id: Mapped[int] = mapped_column(Integer, comment="用户ID")
    status: Mapped[str] = mapped_column(String(20), default="pending", comment="订单状态: pending/paid/shipped/completed/cancelled/returning/returned")
    total_amount: Mapped[float] = mapped_column(Float, default=0, comment="总金额")
    pay_method: Mapped[str] = mapped_column(String(20), nullable=True, comment="支付方式: wechat/alipay")
    address_snapshot: Mapped[str] = mapped_column(Text, nullable=True, comment="收货地址快照(JSON)")
    items_snapshot: Mapped[str] = mapped_column(Text, nullable=True, comment="商品快照(JSON)")
    remark: Mapped[str] = mapped_column(String(500), nullable=True, comment="备注")
    # 物流与售后
    tracking_no: Mapped[str] = mapped_column(String(100), nullable=True, comment="发货快递单号")
    return_tracking_no: Mapped[str] = mapped_column(String(100), nullable=True, comment="退货快递单号")
    exchange_tracking_no: Mapped[str] = mapped_column(String(100), nullable=True, comment="换货新快递单号")
    return_status: Mapped[str] = mapped_column(String(20), nullable=True, comment="售后状态: none/returning/refunded/exchanged")
    return_reason: Mapped[str] = mapped_column(String(500), nullable=True, comment="退换货原因")
    return_applied_at: Mapped[datetime] = mapped_column(DateTime, nullable=True)
    return_completed_at: Mapped[datetime] = mapped_column(DateTime, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
    paid_at: Mapped[datetime] = mapped_column(DateTime, nullable=True)
    shipped_at: Mapped[datetime] = mapped_column(DateTime, nullable=True)
    completed_at: Mapped[datetime] = mapped_column(DateTime, nullable=True)
    cancelled_at: Mapped[datetime] = mapped_column(DateTime, nullable=True)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `order_no` | `String(64)` | `UNIQUE` | 订单号 |
| `user_id` | `Integer` | `NOT NULL` | 下单用户 ID |
| `status` | `String(20)` | 默认 `pending` | 订单状态 |
| `total_amount` | `Float` | 默认 `0` | 总金额 |
| `pay_method` | `String(20)` | 可空 | 支付方式 |
| `address_snapshot` | `Text` | 可空 | 收货地址 JSON 快照 |
| `items_snapshot` | `Text` | 可空 | 商品 JSON 快照 |
| `remark` | `String(500)` | 可空 | 备注 |
| `tracking_no` | `String(100)` | 可空 | 发货快递单号 |
| `return_tracking_no` | `String(100)` | 可空 | 退货快递单号 |
| `exchange_tracking_no` | `String(100)` | 可空 | 换货新快递单号 |
| `return_status` | `String(20)` | 可空 | 售后状态 |
| `return_reason` | `String(500)` | 可空 | 退换货原因 |
| `return_applied_at` | `DateTime` | 可空 | 申请售后时间 |
| `return_completed_at` | `DateTime` | 可空 | 售后完成时间 |
| `created_at` | `DateTime` | 默认当前时间 | 下单时间 |
| `paid_at` | `DateTime` | 可空 | 支付时间 |
| `shipped_at` | `DateTime` | 可空 | 发货时间 |
| `completed_at` | `DateTime` | 可空 | 完成时间 |
| `cancelled_at` | `DateTime` | 可空 | 取消时间 |

### 3.9 order_items（订单商品明细）

```python
# backend/models/shopping.py

class OrderItem(Base):
    """订单商品"""
    __tablename__ = "order_items"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    order_id: Mapped[int] = mapped_column(Integer, ForeignKey("orders.id"), comment="订单ID")
    product_id: Mapped[int] = mapped_column(Integer, comment="商品ID")
    product_name: Mapped[str] = mapped_column(String(500), comment="商品名称快照")
    price: Mapped[float] = mapped_column(Float, default=0, comment="购买时价格")
    quantity: Mapped[int] = mapped_column(Integer, default=1, comment="数量")
    image_url: Mapped[str] = mapped_column(String(500), nullable=True, default="", comment="商品图片快照")
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `order_id` | `Integer` | `ForeignKey("orders.id")` | 所属订单 |
| `product_id` | `Integer` | `NOT NULL` | 商品 ID |
| `product_name` | `String(500)` | `NOT NULL` | 购买时商品名称快照 |
| `price` | `Float` | 默认 `0` | 购买时价格 |
| `quantity` | `Integer` | 默认 `1` | 数量 |
| `image_url` | `String(500)` | 可空，默认空 | 商品图片快照 |

### 3.10 customer_service_messages（客服消息）

```python
# backend/models/customer_service.py

class CustomerServiceMessage(Base):
    __tablename__ = "customer_service_messages"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False, index=True)
    product_id: Mapped[int] = mapped_column(Integer, ForeignKey("products.id"), nullable=True)
    sender_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=True)
    sender_role: Mapped[str] = mapped_column(String(16), default="user")  # user / merchant / ai
    content: Mapped[str] = mapped_column(Text, default="")
    is_read: Mapped[bool] = mapped_column(Boolean, default=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `user_id` | `Integer` | `ForeignKey("users.id")`、`NOT NULL`、`index=True` | 会话所属用户 |
| `product_id` | `Integer` | `ForeignKey("products.id")`、可空 | 关联商品 |
| `sender_id` | `Integer` | `ForeignKey("users.id")`、可空 | 发送者用户 |
| `sender_role` | `String(16)` | 默认 `user` | 发送者角色：`user` / `merchant` / `ai` |
| `content` | `Text` | 默认空 | 消息内容 |
| `is_read` | `Boolean` | 默认 `False` | 是否已读 |
| `created_at` | `DateTime` | 默认当前时间 | 发送时间 |

### 3.11 knowledge_entries（知识库条目）

```python
# backend/models/knowledge_base.py

class KnowledgeEntry(Base):
    """知识库条目 - 商品参数/规格/售后/FAQ 向量化存储"""
    __tablename__ = "knowledge_entries"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    product_id: Mapped[int] = mapped_column(Integer, nullable=True, comment="关联商品ID")
    category: Mapped[str] = mapped_column(String(50), comment="知识类型: spec/faq/after_sale/policy")
    title: Mapped[str] = mapped_column(String(500), comment="条目标题")
    content: Mapped[str] = mapped_column(Text, comment="知识内容")
    keywords: Mapped[str] = mapped_column(Text, nullable=True, comment="关键词(JSON)")
    vector_data: Mapped[str] = mapped_column(Text, nullable=True, comment="简化向量数据(JSON)")
    is_active: Mapped[bool] = mapped_column(default=True, comment="是否启用")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now, onupdate=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `product_id` | `Integer` | 可空 | 关联商品 ID |
| `category` | `String(50)` | `NOT NULL` | 知识类型 |
| `title` | `String(500)` | `NOT NULL` | 条目标题 |
| `content` | `Text` | `NOT NULL` | 知识内容 |
| `keywords` | `Text` | 可空 | 关键词 JSON |
| `vector_data` | `Text` | 可空 | 简化向量数据 JSON |
| `is_active` | `Boolean` | 默认 `True` | 是否启用 |
| `created_at` | `DateTime` | 默认当前时间 | 创建时间 |
| `updated_at` | `DateTime` | 默认并更新时刷新 | 更新时间 |

### 3.12 qa_records（用户问答记录）

```python
# backend/models/knowledge_base.py

class QARecord(Base):
    """用户问答记录 - 记录用户提问和AI回答"""
    __tablename__ = "qa_records"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, nullable=True, comment="用户ID")
    product_id: Mapped[int] = mapped_column(Integer, nullable=True, comment="关联商品ID")
    question: Mapped[str] = mapped_column(Text, comment="用户问题")
    answer: Mapped[str] = mapped_column(Text, comment="AI回答")
    question_type: Mapped[str] = mapped_column(String(50), default="auto", comment="问题类型")
    source: Mapped[str] = mapped_column(String(50), default="rag", comment="回答来源: rag/template/mock")
    helpful: Mapped[int] = mapped_column(Integer, default=0, comment="有用数")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
```

| 字段名 | 类型 | 约束 | 注释 |
|--------|------|------|------|
| `id` | `Integer` | 主键自增 | — |
| `user_id` | `Integer` | 可空 | 用户 ID |
| `product_id` | `Integer` | 可空 | 关联商品 ID |
| `question` | `Text` | `NOT NULL` | 用户问题 |
| `answer` | `Text` | `NOT NULL` | AI 回答 |
| `question_type` | `String(50)` | 默认 `auto` | 问题类型 |
| `source` | `String(50)` | 默认 `rag` | 回答来源 |
| `helpful` | `Integer` | 默认 `0` | 有用数 |
| `created_at` | `DateTime` | 默认当前时间 | 创建时间 |

---

## 4. 关键关联关系

### 4.1 User 与 Order

一个用户可以拥有多个订单，关系在 `Order.user_id` 上以普通整数字段表达，未显式建立外键约束，但业务层通过用户 ID 查询订单。

```python
# backend/models/shopping.py
class Order(Base):
    ...
    user_id: Mapped[int] = mapped_column(Integer, comment="用户ID")
```

### 4.2 Product 与 Review

评论表通过 `product_id` 外键关联商品，并配置 `relationship` 与级联删除：删除商品时会同步删除其下所有评论。

```python
# backend/models/product.py
class Product(Base):
    ...
    reviews = relationship("Review", back_populates="product", cascade="all, delete-orphan")
```

```python
# backend/models/review.py
class Review(Base):
    ...
    product_id: Mapped[int] = mapped_column(Integer, ForeignKey("products.id"), comment="关联商品ID")
    product = relationship("Product", back_populates="reviews")
```

### 4.3 Order 与 OrderItem

订单与订单商品明细为一对多关系，删除订单时级联删除其明细。

```python
# backend/models/shopping.py
class Order(Base):
    ...
    items = relationship("OrderItem", back_populates="order", cascade="all, delete-orphan")

class OrderItem(Base):
    ...
    order_id: Mapped[int] = mapped_column(Integer, ForeignKey("orders.id"), comment="订单ID")
    order = relationship("Order", back_populates="items")
```

### 4.4 CustomerServiceMessage 与 User / Product

客服消息同时关联“会话所属用户”与“发送者用户”两个用户维度，以及可选的商品维度。

```python
# backend/models/customer_service.py
class CustomerServiceMessage(Base):
    ...
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False, index=True)
    sender_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=True)
    product_id: Mapped[int] = mapped_column(Integer, ForeignKey("products.id"), nullable=True)

    user = relationship("User", foreign_keys=[user_id])
    sender = relationship("User", foreign_keys=[sender_id])
    product = relationship("Product")
```

### 4.5 Product 与 KnowledgeEntry / QARecord

知识库条目与问答记录均通过 `product_id` 与商品建立弱关联（模型字段可空，未显式声明外键约束），用于 RAG 检索与问答溯源。

```python
# backend/models/knowledge_base.py
class KnowledgeEntry(Base):
    product_id: Mapped[int] = mapped_column(Integer, nullable=True, comment="关联商品ID")

class QARecord(Base):
    product_id: Mapped[int] = mapped_column(Integer, nullable=True, comment="关联商品ID")
```

---

## 5. 迁移策略

项目未使用 Alembic 等版本化迁移工具，而是在 `backend/database.py` 的 `init_db()` 中通过 SQLAlchemy 反射 + 原生 `ALTER TABLE` 实现增量字段补齐与数据迁移。该策略适合小型项目或 MVP 阶段，能够保证旧数据库结构平滑升级。

### 5.1 初始化入口

```python
# backend/database.py

def init_db() -> None:
    """创建所有表并执行兼容性迁移"""
    import backend.models  # noqa: F401
    Base.metadata.create_all(bind=engine)
    # 1. 先补齐所有缺失的数据库字段（避免后续数据迁移因字段不存在而失败）
    _add_password_plain_column()
    _add_product_media_columns()
    _add_review_media_columns()
    _add_product_published_column()
    _add_order_extra_columns()
    _add_display_id_columns()
    _add_review_user_id_column()
    _add_cs_sender_id_column()
    # 2. 数据迁移：将残留明文密码哈希化并清空明文
    _hash_existing_plain_passwords()
    # 3. 规范化已有订单编号
    _normalize_order_numbers()
```

执行顺序遵循“先结构后数据”原则：

1. 先补齐所有缺失列，避免后续数据迁移读取到不存在的字段。
2. 将历史明文密码迁移为哈希。
3. 规范化历史订单编号。

### 5.2 字段补齐迁移

#### 5.2.1 用户表 password_plain

用于兼容旧版本存在明文密码的库，迁移完成后该字段会被清空。SQLite 与 PostgreSQL 均使用 `VARCHAR(100)`。

```python
def _add_password_plain_column() -> None:
    """为已有 users 表添加 password_plain 字段（如果不存在）"""
    try:
        from sqlalchemy import inspect, text
        inspector = inspect(engine)
        if "users" in inspector.get_table_names():
            columns = [c["name"] for c in inspector.get_columns("users")]
            if "password_plain" not in columns:
                with engine.connect() as conn:
                    if DATABASE_URL.startswith("sqlite"):
                        conn.execute(text("ALTER TABLE users ADD COLUMN password_plain VARCHAR(100)"))
                    else:
                        conn.execute(text("ALTER TABLE users ADD COLUMN password_plain VARCHAR(100)"))
                    conn.commit()
                logger.info("已为 users 表添加 password_plain 字段")
    except Exception as e:
        logger.warning(f"添加 password_plain 字段失败: {e}")
```

#### 5.2.2 商品与评论媒体字段

分别为 `products` 与 `reviews` 添加 `image_urls` 与 `videos`。PostgreSQL 使用 `JSONB`，SQLite 使用 `JSON`。

```python
def _add_product_media_columns() -> None:
    """为已有 products 表添加 image_urls / videos 字段（如果不存在）"""
    ...
    if "image_urls" not in columns:
        if DATABASE_URL.startswith("sqlite"):
            conn.execute(text("ALTER TABLE products ADD COLUMN image_urls JSON"))
        else:
            conn.execute(text("ALTER TABLE products ADD COLUMN image_urls JSONB"))
```

```python
def _add_review_media_columns() -> None:
    """为已有 reviews 表添加 image_urls / videos 字段（如果不存在）"""
    ...
```

#### 5.2.3 商品上架状态

为商品添加 `is_published`，并将已有的 `NULL` 回填为 `True`。

```python
def _add_product_published_column() -> None:
    """为已有 products 表添加 is_published 字段，并将现有 NULL 回填为 True"""
    ...
    with SessionLocal() as db:
        from backend.models.product import Product
        updated = (
            db.query(Product)
            .filter(Product.is_published.is_(None))
            .update({"is_published": True}, synchronize_session=False)
        )
        db.commit()
```

#### 5.2.4 订单扩展字段

包含物流单号、售后状态及多个状态时间戳。针对 PostgreSQL 使用 `TIMESTAMP`，SQLite 使用 `DateTime`。

```python
def _add_order_extra_columns() -> None:
    """为已有 orders 表添加物流、售后与状态时间字段（如果不存在）"""
    col_defs = [
        ("tracking_no", "VARCHAR(100)"),
        ("return_tracking_no", "VARCHAR(100)"),
        ("exchange_tracking_no", "VARCHAR(100)"),
        ("return_status", "VARCHAR(20)"),
        ("return_reason", "VARCHAR(500)"),
        ("return_applied_at", "TIMESTAMP" if is_pg else "DateTime"),
        ("return_completed_at", "TIMESTAMP" if is_pg else "DateTime"),
        ("paid_at", "TIMESTAMP" if is_pg else "DateTime"),
        ("shipped_at", "TIMESTAMP" if is_pg else "DateTime"),
        ("completed_at", "TIMESTAMP" if is_pg else "DateTime"),
        ("cancelled_at", "TIMESTAMP" if is_pg else "DateTime"),
    ]
```

#### 5.2.5 展示 ID 与用户/客服字段

- `_add_display_id_columns()`：为 `users` 与 `products` 添加 `display_id`，并按日期/计数顺序回填缺失值。
- `_add_review_user_id_column()`：为 `reviews` 添加 `user_id`。
- `_add_cs_sender_id_column()`：为 `customer_service_messages` 添加 `sender_id`。

### 5.3 数据迁移

#### 5.3.1 明文密码哈希化

该函数通过原生 SQL 读取 `password_plain`，调用 `auth_service.hash_password` 生成新哈希写入 `password_hash`，并清空明文。

```python
def _hash_existing_plain_passwords() -> None:
    """将现有 users.password_plain 明文密码哈希化到 password_hash，并清空 password_plain。"""
    ...
    with engine.connect() as conn:
        rows = conn.execute(
            text(
                "SELECT id, password_plain FROM users "
                "WHERE password_plain IS NOT NULL AND password_plain != ''"
            )
        ).fetchall()
        for user_id, plain in rows:
            new_hash = hash_password(plain.strip())
            conn.execute(
                text(
                    "UPDATE users SET password_hash = :h, password_plain = NULL WHERE id = :id"
                ),
                {"h": new_hash, "id": user_id},
            )
        conn.commit()
```

#### 5.3.2 订单编号规范化

将不符合 `^\d{14}\d{5}\d{5}$` 规范的订单号统一重命名为：`YYYYMMDDHHMMSS<商品展示ID><5位序号>`。

```python
def _normalize_order_numbers() -> None:
    """将数据库中不符合规范的订单编号统一为：YYYYMMDDHHMMSS<商品展示ID><5位序号>"""
    valid_pattern = re.compile(r"^\d{14}\d{5}\d{5}$")
    ...
    ts = order.created_at.strftime("%Y%m%d%H%M%S") if order.created_at else datetime.now().strftime("%Y%m%d%H%M%S")
    base = f"{ts}{display_id}"
    seq = counters.get(base, 1)
    while True:
        new_no = f"{base}{seq:05d}"
        if new_no not in used_nos:
            break
        seq += 1
    order.order_no = new_no
```

### 5.4 迁移策略说明

- 所有迁移函数均为幂等：先检查列/表是否存在，不存在才执行。
- 异常仅记录警告日志，不中断启动流程。
- 字段类型根据当前数据库类型动态选择 `JSON`/`JSONB`、`TIMESTAMP`/`DateTime`。
- 建议后续项目规模扩大后引入 Alembic，替代手写 `ALTER TABLE` 迁移。

---

## 6. 索引设计

当前项目主要依赖主键与显式唯一约束，以及一处业务查询索引。具体如下：

### 6.1 主键索引

所有表的主键 `id` 均由数据库自动创建聚集/非聚集索引。

### 6.2 唯一索引

| 表 | 字段 | 说明 |
|----|------|------|
| `users` | `username` | 登录账号唯一 |
| `users` | `display_id` | 用户展示 ID 唯一 |
| `products` | `display_id` | 商品展示 ID 唯一 |
| `orders` | `order_no` | 订单号唯一 |

### 6.3 普通索引

| 表 | 字段 | 说明 |
|----|------|------|
| `customer_service_messages` | `user_id` | 按用户查询会话消息时加速检索 |

### 6.4 建议补充的索引

随着数据量增长，以下字段组合建议增加索引以提升查询性能：

- `orders(user_id, status)`：用户订单列表按状态筛选。
- `orders(created_at)`：后台订单排序与分页。
- `reviews(product_id, created_at)`：商品详情页评论列表。
- `cart_items(user_id, product_id)`：购物车唯一性与查询。
- `user_favorites(user_id, product_id)`：避免重复收藏并加速查询。
- `browse_histories(user_id, created_at)`：个人中心浏览历史。
- `customer_service_messages(user_id, created_at)`：客服会话按时间排序。
- `knowledge_entries(product_id, category)`：按商品与知识类型检索。
- `qa_records(user_id, created_at)` / `qa_records(product_id, created_at)`：问答记录查询。

目前代码中这些字段未显式声明索引，相关查询通过全表扫描完成；在小数据量场景下可接受，正式上线前建议根据实际查询频率补充。

---

## 7. 外键约束与用户删除策略

### 7.1 关联关系梳理

`users` 表是系统的核心实体，多张业务表直接或间接引用它：

| 引用表 | 引用字段 | 关联含义 | 删除策略 |
|--------|----------|----------|----------|
| `user_addresses` | `user_id` | 用户收货地址 | 随用户删除一并清理 |
| `user_favorites` | `user_id` | 用户收藏 | 随用户删除一并清理 |
| `browse_histories` | `user_id` | 浏览历史 | 随用户删除一并清理 |
| `cart_items` | `user_id` | 购物车 | 随用户删除一并清理 |
| `customer_service_messages` | `user_id` | 客服消息 | 随用户删除一并清理 |
| `orders` | `user_id` | 用户订单 | 随用户删除一并清理 |
| `order_items` | `order_id` | 订单商品 | 先删除订单商品再删除订单 |
| `reviews` | `user_id` | 评论 | 置空 `user_id`，保留评论内容 |
| `qa_records` | `user_id` | 问答记录 | 置空 `user_id`，保留问答内容 |

### 7.2 删除用户时的清理顺序

`backend/api/merchant_routes.py` 中的 `delete_user` 按以下顺序执行，避免违反外键约束：

```python
# backend/api/merchant_routes.py
# 1. 清理与用户直接关联的弱业务数据
db.execute(delete(CartItem).where(CartItem.user_id == user_id))
db.execute(delete(UserFavorite).where(UserFavorite.user_id == user_id))
db.execute(delete(BrowseHistory).where(BrowseHistory.user_id == user_id))
db.execute(delete(UserAddress).where(UserAddress.user_id == user_id))
db.execute(delete(CustomerServiceMessage).where(CustomerServiceMessage.user_id == user_id))

# 2. 对需要保留历史记录的表脱敏
db.execute(update(QARecord).where(QARecord.user_id == user_id).values(user_id=None))
db.execute(update(Review).where(Review.user_id == user_id).values(user_id=None))

# 3. 清理强关联订单数据
order_ids = [row[0] for row in db.execute(select(Order.id).where(Order.user_id == user_id)).all()]
if order_ids:
    db.execute(delete(OrderItem).where(OrderItem.order_id.in_(order_ids)))
    db.execute(delete(Order).where(Order.id.in_(order_ids)))

# 4. 删除用户
db.delete(target_user)
db.commit()
```

### 7.3 设计说明

- 所有删除操作在同一个 `SessionLocal` 会话中完成，只有最后一步执行 `commit`，保证原子性。
- `reviews` 与 `qa_records` 属于平台运营数据，直接删除会导致统计报表失真，因此采用“置空用户关联但保留内容”的软脱敏策略。
- 订单属于用户私有业务数据，用户删除后订单失去业务归属，因此连同 `order_items` 一并清理。
- 该接口仍禁止商家删除自身账号，防止误操作导致当前会话失效。
