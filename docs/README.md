# 文档目录

- `architecture.md`：当前系统架构、模块职责、迁移与部署说明。
- `generated/`：自动生成或导出的静态技术报告，属于可版本化项目资料。
- `submission/`：本地课程提交材料和办公文档，已由根目录 `.gitignore` 排除。

运行方法、默认账号和最短验证命令请从根目录 [README](../README.md) 开始。不要把数据库备份、运行缓存或新的压缩导出包放在项目根目录；数据库备份统一放入已忽略的 `data/backups/`。
