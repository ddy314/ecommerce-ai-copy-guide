# Ecommerce AI Copy Guide Frontend

Vue 3 + Vite 前端，用于展示电商 AI 商品文案生成与智能导购助手的初始化页面。

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Environment

默认后端地址为 `http://localhost:8000`。如需覆盖，在 `frontend/.env.local` 中配置：

```text
VITE_API_BASE_URL=http://localhost:8000
```
